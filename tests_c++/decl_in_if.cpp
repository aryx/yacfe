void main()
{

    if (int tok2= shift_token(&text, &word_len)) {

	}

    if (Token tok2= shift_token(&text, &word_len)) {
	}


    if (Command *com= parse_command(packet))
		{ 
		}
	if (int err = cert.GetError().What())
		{
		}
    if (x509* cert = list_.front()) {
	}


}
