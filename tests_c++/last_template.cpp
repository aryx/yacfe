
void main()
{
	int x;
	vector<X> x;
	vector2<Y*> x;

	std::vector3<Z> x;

	std::vector4<Macro*> ms;
	std::vector5<ObjectHolder*> os;
	vector6<kvoctrainExpr>::const_iterator first =  vocabulary.begin ();
 	QValueList<GlossaryItem*>::iterator it = m_itemlist.begin();

	vector8<int>::Iterator it;

	QMap<QString, Region>::Iterator it;
    //QMap<QString, Region>::Iterator it;
}
