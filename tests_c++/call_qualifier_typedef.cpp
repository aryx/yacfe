// cos typdef infered in previous line and currently do not allow
// Ttypedef::  cos get ambiguity otherwise

void main()
{

     nsAccessibleWrap *accWrap = GetAccessibleWrap(aAtkObj);
     if (!accWrap || nsAccessibleWrap::MustPrune(accWrap)) {
	 }

}
