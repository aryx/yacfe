#define MODULE_PARM_DESC(a, b) (b + a)


MODULE_PARM_DESC(type,	   "You can set this to specific card type (doesn't " \
		 "work with pnp)");

MODULE_PARM_DESC(type,	   "You can set this to specific card type (doesn't " 
		 "work with pnp)");
